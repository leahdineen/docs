---
title: 'List Judges'
api: 'GET /judges'
description: 'Retrieve a paginated list of judges'
---

## Overview

Retrieves a paginated list of judges with optional filtering by content type and status.

## Query Parameters

<ParamField query="limit" type="integer" default="50">
  Number of judges to return (max 100)
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of judges to skip
</ParamField>

<ParamField query="content_type" type="string">
  Filter by content type (text, video, audio, image)
</ParamField>

<ParamField query="status" type="string">
  Filter by status (ready, learning, training)
</ParamField>

## Response

<ResponseField name="judges" type="array" required>
  Array of judge objects
</ResponseField>

<ResponseField name="total" type="integer" required>
  Total number of judges available
</ResponseField>

<ResponseField name="has_more" type="boolean" required>
  Whether more judges are available
</ResponseField>

<ResponseField name="limit" type="integer">
  Number of judges returned
</ResponseField>

<ResponseField name="offset" type="integer">
  Number of judges skipped
</ResponseField>

## Examples

<CodeGroup>

```bash Request
curl -X GET "https://api.getpique.ai/judges?limit=20&content_type=text" \
  -H "X-API-Key: sk_org_xxxxxxxxx"
```

```json Success Response
{
  "judges": [
    {
      "id": "jdg_xyz789",
      "name": "Content Quality Judge",
      "description": "Evaluates content for clarity and accuracy",
      "content_type": "text",
      "status": "ready",
      "training_progress": 100,
      "criteria": [
        {
          "id": "crit_001",
          "name": "Clarity",
          "description": "How clear and understandable the content is"
        }
      ],
      "created_at": "2024-01-10T10:00:00Z",
      "updated_at": "2024-01-14T15:30:00Z",
      "version_number": 1,
      "is_active": true
    }
  ],
  "total": 15,
  "has_more": false,
  "limit": 20,
  "offset": 0
}
```

</CodeGroup>